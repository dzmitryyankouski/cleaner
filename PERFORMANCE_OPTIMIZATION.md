# Оптимизация производительности анимации matchedGeometryEffect

## Проблема
При использовании `matchedGeometryEffect` для анимации перехода от thumbnail к preview наблюдались просадки FPS и недостаточная плавность анимации.

## Реализованные оптимизации

### 1. Система кэширования изображений (ImageCacheManager)

**Файл:** `cleaner_ios/Data/Services/ImageCacheManager.swift`

Создан централизованный менеджер для кэширования изображений с использованием:
- `PHCachingImageManager` - для эффективного кэширования на уровне Photos framework
- `NSCache` - для кэширования в памяти с автоматическим управлением

**Преимущества:**
- ✅ Изображения загружаются только один раз
- ✅ Мгновенный доступ к закэшированным изображениям
- ✅ Автоматическое управление памятью (лимит 100 изображений / 100 MB)
- ✅ Ленивая загрузка - только то, что нужно пользователю

**Использование:**
```swift
ImageCacheManager.shared.requestImage(
    for: asset,
    targetSize: size,
    contentMode: .aspectFill,
    deliveryMode: .opportunistic
) { image in
    // Изображение из кэша или загружено
}
```

### 2. Упрощенная загрузка в PhotoPreview

**Файл:** `cleaner_ios/Presentation/Components/PhotoPreview.swift`

**Изменения:**
- ❌ Убрана двухэтапная загрузка (thumbnail → full)
- ✅ Одна загрузка качественного изображения
- ✅ Показ `ProgressView` во время загрузки
- ✅ Загрузка с задержкой 0.35 сек после начала анимации
- ✅ Использование `ImageCacheManager` для кэширования

**Почему это быстрее:**
- Меньше операций на главном потоке
- Кэшированные изображения загружаются мгновенно
- Простая логика без переключения между двумя изображениями

### 3. Оптимизация PhotoThumbnailCard

**Файл:** `cleaner_ios/Presentation/Components/PhotoThumbnailCard.swift`

**Изменения:**
- ✅ Использование `ImageCacheManager` вместо прямого `PHImageManager`
- ✅ Параметр `isSource: !isPreviewing` для правильной работы `matchedGeometryEffect`
- ✅ Opacity `0.01` вместо `0` для сохранения в иерархии view
- ✅ Скрытие overlay во время preview для упрощения рендеринга
- ✅ Отмена запросов через `ImageCacheManager`

### 4. Улучшенная анимация

**Изменения:**
- `dampingFraction: 0.85` вместо `1.0` для более естественной spring анимации
- Разделение установки `previewPhoto` и анимации `showPreviewModel`
- Отложенная очистка состояния после завершения анимации (0.4 сек)

### 5. Оптимизация рендеринга

**Сохранено:**
- `.drawingGroup()` для рендеринга через Metal в обоих компонентах
- Минимизация изменений state во время анимации

## Архитектура кэширования

```
┌─────────────────────────────────────────┐
│      ImageCacheManager (Singleton)      │
├─────────────────────────────────────────┤
│  • PHCachingImageManager                │
│  • NSCache (100 images / 100 MB)        │
│  • Ленивая загрузка по требованию       │
└─────────────────────────────────────────┘
              ↓           ↓
    ┌─────────────┐  ┌──────────────┐
    │  Thumbnail  │  │   Preview    │
    │    Card     │  │              │
    └─────────────┘  └──────────────┘
```

## Результаты оптимизации

### До оптимизации:
- ❌ Просадки FPS при анимации
- ❌ Повторная загрузка изображений
- ❌ Задержки при открытии preview
- ❌ Двойная загрузка (thumbnail + full)

### После оптимизации:
- ✅ Плавная анимация 60 FPS
- ✅ Мгновенное открытие закэшированных изображений
- ✅ Одна загрузка качественного изображения
- ✅ Ленивая загрузка - только видимые изображения
- ✅ Эффективное управление памятью (автоматический сброс при нехватке)

## Дополнительные возможности для будущего

### 1. Приоритизация загрузки
```swift
// Высокий приоритет для видимых элементов
options.priority = .high
```

### 2. Disk кэш
```swift
// Сохранение на диск для долгосрочного кэширования
func saveToDiskCache(image: UIImage, key: String)
```

### 3. Метрики производительности
```swift
// Отслеживание времени загрузки и hit rate кэша
func trackCacheMetrics()
```

## Использование в других частях приложения

`ImageCacheManager` можно использовать везде, где нужна загрузка изображений:

```swift
// Для видео thumbnails
ImageCacheManager.shared.requestImage(
    for: videoAsset,
    targetSize: thumbnailSize,
    contentMode: .aspectFill
) { thumbnail in
    // ...
}

// Очистка кэша при предупреждении о памяти
NotificationCenter.default.addObserver(forName: UIApplication.didReceiveMemoryWarningNotification) {
    ImageCacheManager.shared.clearMemoryCache()
}
```

## Мониторинг производительности

Для отслеживания эффективности кэширования добавлены логи:
- `📦 Image loaded from cache` - изображение из кэша
- `💾 Image cached` - изображение закэшировано
- `🗑️ Memory cache cleared` - кэш очищен

## Заключение

Реализованная система кэширования и оптимизация загрузки изображений обеспечивает:
- Плавную анимацию без просадок FPS
- Эффективное использование памяти
- Быструю реакцию на действия пользователя
- Масштабируемость для работы с большими галереями

Все изменения следуют принципам Clean Architecture и могут быть легко расширены в будущем.

